git submodule update --init extern/pybind11
After adding new algos and finising it(algo.run()), do that:
# rebuild (In Project root directory)
rm -rf src/cpp/build
mkdir -p src/cpp/build
cd build
cmake .. # 如果在cache裡面已經存在其他CMakeCache.txt 會無法make 需要先將其刪掉(把其他在電腦中專案路徑的build刪除)              
make -j         
cd ..

# Test in tests folder...
python tests/algo_test.py
